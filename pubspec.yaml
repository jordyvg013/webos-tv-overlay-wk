name: webos_tv_overlay
description: "A Flutter webOS TV Match Overlay Test Project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  video_player: ^2.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Hier koppelen we de mappen die je op GitHub hebt aangemaakt
  assets:
    - assets/video/
    - assets/image/
